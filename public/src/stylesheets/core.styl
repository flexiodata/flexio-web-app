::selection
  background: #3390ff
  color: #fff

*,
*::before,
*::after
  box-sizing: border-box

html
  // necessary for tether dropdown place
  height: 100%

body
  font-family: $font-family
  font-size: $font-size
  // necessary for tether dropdown place
  min-height: 100%
  overflow: hidden
  color: $primary-text

input, textarea, keygen, select, button
  font-family: $font-family

input, button, submit
  border: none
  outline: none

// match Element UI
input
  &.disabled,
  &[disabled]
    background-color: #f5f7fa
    border-color: #e4e7ed
    color: #c0c4cc
    cursor: not-allowed

button, submit
  &.disabled,
  &[disabled]
    pointer-events: none; // future-proof disabling of clicks
    opacity: .65

button
  background: none
  cursor: pointer

  &.disabled,
  &[disabled]
    cursor: default

table
  /* cellspacing */
  border-spacing: 0

th, td
  /* cellpadding */
  padding: 0

p
  line-height: 1.5

p:first-child
  margin-top: 0
