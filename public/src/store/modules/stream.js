const getDefaultState = () => {
  return {
    fetching: false,
    fetched: false
  }
}

const state = getDefaultState()

const mutations = {}

const actions = {}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}

